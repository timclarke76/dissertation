[project]
name = "generate"
version = "0.1.0"
description = ""
authors = [
    {name = "Timothy Clarke"}
]
requires-python = "==3.10.12"
dependencies = [
    "ruff (>=0.15.20,<0.16.0)",
    "torch (==2.3.1)",
    "onnx (==1.16.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
